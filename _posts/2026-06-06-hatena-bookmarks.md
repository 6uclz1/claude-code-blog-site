---
layout: post
title: "はてなブックマーク 2026年06月06日 の記事まとめ (2件)"
date: 2026-06-07 08:59:04 +0900
excerpt: "はてなブックマークで気になった記事をAIで要約してお届けします。2026年06月06日分の2件の記事をまとめました。

- GitHub - microsoft/pg_durable: PostgreSQL in-database durable execution

- CVE-2026-49975「HTTP/2 Bomb」をわかりやすく解説——AIが人間より先に気づいた脆弱性 - Qiita
"
---

はてなブックマークで気になった記事をAIで要約してお届けします。
2026年06月06日分の2件の記事をまとめました。

## 1. GitHub - microsoft/pg_durable: PostgreSQL in-database durable execution

**URL:** [https://github.com/microsoft/pg_durable](https://github.com/microsoft/pg_durable)

### AI要約

**要点**
- PostgreSQLデータベース内で、耐久性のある長時間実行SQL関数を実現します。
- 外部サービスなしで、クラッシュや処理失敗後も自動で前回のチェックポイントから処理を再開します。
- ワークフロー定義、リトライ、進捗管理をデータベース内部に統合します。
- データ・AIパイプライン、システムメンテナンス、外部API連携などの幅広い用途に有効です。

**詳細な要約**
pg_durableは、Microsoftが提供するPostgreSQLデータベース内で持続的な実行（Durable Execution）を可能にするツールです。これは、外部のcronジョブ、ワーカー、キュー、ステータス管理テーブルなどを個別に構築・連携させることなく、データベース内部で長時間にわたる信頼性の高いバックグラウンド処理を実現することを目的としています。

このシステムの核心は、SQLで定義されたワークフローの各ステップをPostgreSQLが実行し、その過程で自動的にチェックポイントを記録する点にあります。これにより、万が一データベースがクラッシュしたり、再起動したり、特定のステップが失敗したりしても、手動で処理の状態を再構築する手間をかけずに、最後に記録されたチェックポイントから処理を自動的に再開できます。

pg_durableは、データを扱うバックエンドやデータエンジニア、DBAやSREなど、データと処理ロジックを密接に配置したいチームに特に適しています。例えば、ベクトル埋め込みパイプライン、データ取り込み処理、定期的なデータベースメンテナンス、並行処理、外部API連携など、多岐にわたるユースケースでその効果を発揮します。ワークフロー定義、リトライ状態、進捗追跡がPostgreSQLに統合されるため、複雑なアプリケーション層のコードや外部オーケストレーターの必要性が減り、運用監視もデータベーステーブルを通じて一元的に行えるようになります。

---

## 2. CVE-2026-49975「HTTP/2 Bomb」をわかりやすく解説——AIが人間より先に気づいた脆弱性 - Qiita

**URL:** [https://qiita.com/long-910/items/76779fc1d8602dab73b3](https://qiita.com/long-910/items/76779fc1d8602dab73b3)

### AI要約

*   「HTTP/2 Bomb」（CVE-2026-49975）は、HTTP/2プロトコルの仕様上の脆弱性を悪用し、遠隔からWebサーバーのメモリを枯渇させ、クラッシュさせる深刻なDoS（サービス拒否）攻撃である。
*   この脆弱性は、OpenAI Codexが複数のWebサーバーのソースコードを解析する過程で、人間のセキュリティ研究者よりも早く発見した点で注目される。
*   攻撃は、HTTP/2のヘッダー圧縮（HPACK）を悪用してサーバー側でメモリを増幅させ、さらにフロー制御によってそのメモリを解放させない仕組みを組み合わせることで実行される。
*   家庭用インターネット回線1本でも、約20秒で32GBのRAMを使い果たすほどの破壊力を持ち、多くの主要Webサーバーが影響を受ける。
*   対策としては、即時のパッチ適用、HTTP/2の無効化、Web Application Firewall (WAF) の導入などが推奨されている。

CVE-2026-49975、通称「HTTP/2 Bomb」は、HTTP/2プロトコルの特性を悪用し、遠隔からWebサーバーのメモリを大量に消費させてクラッシュさせる重大なDoS（サービス拒否）脆弱性です。この脆弱性は、これまで個別に知られていた「HPACK圧縮によるメモリ増幅」と「HTTP/2フロー制御によるメモリ保持」という二つの技術を組み合わせることで、破壊的な攻撃チェーンを生み出します。

この「HTTP/2 Bomb」の最大の特徴は、OpenAI Codexが複数のWebサーバーのソースコードを解析する過程で、人間の研究者が気づかなかったこの組み合わせ攻撃を自律的に発見した点にあります。AIは、人間が分野ごとの知識に集中しがちな「知識のサイロ化」に囚われず、大量のコードを横断的に比較することで、潜在的な脆弱性を特定しました。

攻撃が成功すると、家庭用インターネット接続1本でも約20秒で32GBのRAMを使い果たすほどの破壊力があり、Shodanのスキャンでは88万件以上のサーバーが脆弱な状態で公開されているとされます。主要なWebサーバーでは、nginxとApache httpdには既にパッチが提供されていますが、Microsoft IISやEnvoy、Cloudflare Pingoraなどは記事公開時点で未パッチでした。緊急の対策として、パッチの適用、HTTP/2の無効化、HTTP/2ヘッダー数の上限設定、接続タイムアウトの短縮、そしてWAF（Web Application Firewall）の導入が推奨されています。AIによる脆弱性発見は増加傾向にあり、今後のセキュリティ対策においてもAIの役割がますます重要になると考えられます。

---

*この記事は、はてなブックマークのRSSフィードから自動生成されました。*  
*要約はAI（Gemini）によって生成されており、元記事の内容を正確に反映していない場合があります。*  
*詳細な内容については、各URLから元記事をご確認ください。*
