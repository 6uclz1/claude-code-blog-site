---
import { SITE } from '../site';
import { withBase } from '../lib/posts';
---

<header>
  <div class="container">
    <a href={withBase('/')} class="site-title">{SITE.title}</a>
  </div>
</header>
